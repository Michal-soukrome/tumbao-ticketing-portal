# Production/staging browser configuration. The anon key is public by design;
# never put a service-role key in a VITE_ variable.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Development only. The app refuses to start a production build with this true.
VITE_TEST_MODE=false
